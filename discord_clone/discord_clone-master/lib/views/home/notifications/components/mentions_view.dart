import 'package:flutter/material.dart';

class MentionsView extends StatelessWidget {
  const MentionsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Column(children: [
      SizedBox(
        height: 10,
        width: 10,
      )
    ]);
  }
}
